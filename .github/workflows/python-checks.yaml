name: Python checks 🐍

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
    paths:
      - "film-catalog/**"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder GH Action step
        run: echo "Hello action!"
